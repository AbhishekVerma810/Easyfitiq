import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TrainerasignexercisePage } from './trainerasignexercise.page';

describe('TrainerasignexercisePage', () => {
  let component: TrainerasignexercisePage;
  let fixture: ComponentFixture<TrainerasignexercisePage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(TrainerasignexercisePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
